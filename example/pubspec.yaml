name: actual_phone_state_example
description: 'Demonstrates how to use the actual_phone_state plugin.'

publish_to: 'none'

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  actual_phone_state:
    path: ../
  permission_handler: ^12.0.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
